{
    "extends": "../core/tsconfig.json",
    "compilerOptions": {
        "baseUrl": ".",
        // checking @serverless-stack/core's dependencies
        // results in error when using TS >= 4.6. This is
        // the default option whe creating an sst v1 app.
        "skipLibCheck": true
    },
    "include": ["lib", "test"]
}